# Python-CLI-Recursive-Directory-Lister-with-Filters
This repository provides a Python CLI tool to recursively list files in a directory with optional file type filtering. Demonstrates file system traversal, os.walk, and command line argument parsing with argparse for efficient directory management and file organization.
